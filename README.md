# g3d

## Estampo

Install the CLI locally with one of:

```bash
pipx install estampo
pip install estampo
uv tool install estampo
```

This repository uses [`estampo.toml`](./estampo.toml) to define a reproducible
3D printing pipeline for the STEP models in the repo root.
